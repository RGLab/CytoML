ifneq (,)
This makefile requires GNU Make.
endif


CXX_STD = CXX11
PKG_CPPFLAGS =-DROUT -I$(LIB_XML2)/$(R_ARCH)/include/libxml2 -I$(LIB_PROTOBUF)/$(R_ARCH)/include -DLIBXML_STATIC
PKG_LIBS = $(LIB_XML2)/$(R_ARCH)/lib/libxml2.a `${R_HOME}/bin/Rscript -e "flowWorkspace:::LdFlags('all')"` `${R_HOME}/bin/Rscript -e "flowCore:::LdFlags()"` -lws2_32

  

