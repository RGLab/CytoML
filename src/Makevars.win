ifneq (,)
This makefile requires GNU Make.
endif


CXX_STD = CXX11
PKG_CPPFLAGS =-DROUT -I$(LIB_XML2)/$(R_ARCH)/include/libxml2 -DLIBXML_STATIC
PKG_LIBS = $(LIB_XML2)/$(R_ARCH)/lib/libxml2.a `${R_HOME}/bin/Rscript -e "flowWorkspace:::LdFlags();cat(' ');RProtoBufLib:::LdFlags();cat(' ');flowCore:::LdFlags()"` -lws2_32

  

