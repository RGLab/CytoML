#  GNUMakefile

# Just a snippet to stop executing under other make(1) commands
# that won't understand these lines
ifneq (,)
This makefile requires GNU Make.
endif


CXX_STD = CXX11
PKG_CPPFLAGS =-DROUT -I/home/wjiang2/mylib/include/libxml2
PKG_LIBS = `${R_HOME}/bin/Rscript -e "flowWorkspace:::LdFlags('all')"` `${R_HOME}/bin/Rscript -e "flowCore:::LdFlags()"` -lxml2

